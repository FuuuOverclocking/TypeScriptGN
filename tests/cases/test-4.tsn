// Test all other expression

a ??
    b && c ||
    1 | 2 & 4 ^ 5 ||
    a == b && a != b ||
    a === b && a !== b;

a < b > c;

key in obj;             // 对象是否含有键
obj instanceof ClassA;  // 对象是否是类的实例

1 << 2 >>> 3;

1 + 2 * 3 - 4 % 5;

2 ** 3 ** 4;            // 幂，唯一右结合算符

true ? 1 : 2;

nodeA -> nodeB -> nodeC; // EventNet 连接符

1, 2;                   // 逗号表达式，求值结果是逗号右边的表达式

// 复合赋值
a += 1;
a *= 1;
a -= 1;
a /= 1;
a <<= 1;
a >>= 1;
a >>>= 1;
a &= 1;
a ^= 1;
a |= 1;
a **= 1;