{
    "extends": "./tsconfig.base.json",
    "compilerOptions": {
        "outDir": "./built", /* Redirect output structure to the directory. */
    },
    "include": [
        "src/**/*"
    ],
    "exclude": [
        "node_modules",
        "**/*.spec.ts",
        "**/*.test.ts"
    ],
}